apiVersion: v1
kind: Service
metadata:
  name: mern-app
  namespace: mern
spec:
  selector:
    app: mern-app
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30050
  type: NodePort
